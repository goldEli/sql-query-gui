import React from "react";
import styled from "styled-components";

interface SqlEditorProps {}

const SqlEditor: React.FC<SqlEditorProps> = (props) => {
  return <Box>SqlEditor</Box>;
};

const Box = styled.div`
  width: 100%;
  min-height: 300px;
`;

export default SqlEditor;
